# A YouTube Metadata Extractor
Collect's Meta Data specified from A YouTube Channel and writes to a `.txt` file. Requires YouTube API Key.

## How to Run

To start the project, run:

```bash
npm run start